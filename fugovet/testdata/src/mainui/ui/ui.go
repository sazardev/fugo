// Package ui is a scaffolded UI package missing its Build function — the
// negative fixture for the uiimport analyzer.
package ui

// Home is here so the package isn't empty, but there's no exported Build.
func Home() string { return "home" }
