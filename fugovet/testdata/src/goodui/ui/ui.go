// Package ui is a properly scaffolded UI package — the positive fixture for
// the uiimport analyzer (no diagnostic expected).
package ui

// Build is the app's root.
func Build() string { return "root" }
